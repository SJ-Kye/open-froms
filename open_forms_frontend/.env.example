# 프론트엔드 환경 변수 (Vite). 필요할 때만 `cp .env.example .env` 로 만들면 됩니다.
#
# 기본값은 `/api` 이며, 개발 서버가 vite.config.ts 의 프록시로 http://localhost:8080 에 넘깁니다.
# 브라우저 입장에서 동일 출처가 되어 CORS 설정이 필요 없습니다.
# 백엔드를 다른 호스트에서 띄우는 등 프록시를 우회해야 할 때만 절대 URL 로 지정하세요.
# VITE_API_BASE_URL=http://localhost:8080/api
