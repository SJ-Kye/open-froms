# 빌드 컨텍스트는 리포지터리 루트입니다(백엔드 Gradle 이 ../docs/05-api-design.md 를
# 태스크 입력으로 선언하므로 백엔드 디렉터리만으로는 컨텍스트가 닫히지 않습니다).
# 그만큼 불필요한 파일이 딸려 들어가기 쉬워 여기서 넉넉히 잘라냅니다.

.git
.github
.idea
.claude

# 빌드 산출물 — 이미지 안에서 새로 빌드하므로 호스트 산출물은 넣지 않습니다.
open_forms_backend/build
open_forms_backend/.gradle
open_forms_frontend/node_modules
open_forms_frontend/dist

# 비밀값
.env
.env.local

# 과제 원본 요건 문서(untracked) 및 산출물
*.pdf
dev-docs
diagram.png
